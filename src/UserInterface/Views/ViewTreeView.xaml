<Window x:Class="UserInterface.Views.ViewTreeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserInterface.Views"
        mc:Ignorable="d"
        Title="Tree View" 
        Height="350" 
        Width="600" 
        Background="#252425"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock FontSize="35" Margin="0, 20, 0, 35" Foreground="White" 
                       VerticalAlignment="Center" HorizontalAlignment="Center">
                <Run Text="{Binding TreeName, UpdateSourceTrigger=PropertyChanged}"/>
                <Run Text="Tree"/>
            </TextBlock>
            <Button x:Name="UpdateStatistics" Content="Get Statistics" Style="{StaticResource ButtonStyle}"
                    MinWidth="200"/>
            <TextBlock Text="{Binding ReportMessage, UpdateSourceTrigger=PropertyChanged}"
                       Margin="0, 25, 0, 0" FontSize="18" Foreground="White" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
